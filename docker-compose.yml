services:
  redis:
    image: redis:latest
    ports:
      - "6380:6379"   
    restart: unless-stopped

  api:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - redis
    volumes:
      - ./src:/src      